name: Main

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build base & bootstrap
        run: |
          cd tofu
          tar cvf base.tar.gz .
          cd ../tofu-bootstrap
          tar cvf bootstrap.tar.gz .
      - name: Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            tofu/base.tar.gz
            tofu-bootstrap/bootstrap.tar.gz
          name: Stable
